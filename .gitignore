__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# Local assistant/workspace scratch
.claude/
.agents/
/PRIVATE.md

# Curated release-upload staging (large installers, built locally, uploaded by hand)
/_github_assets/
/_github_assets_beta/

# Local investigation scratch (replays, samples, throwaway scripts)
/work_in_progress/

# Replay-corpus statistics: regenerated from a local folder of .replay files, large
# (parse cache + per-unit fact tables), and derived from data that is not in the repo.
/reports/replay_stats/

